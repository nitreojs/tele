From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: nitreojs <hello@starkow.dev>
Date: Sat, 26 Sep 2026 18:39:44 +0300
Subject: [PATCH] feat: add an option to hide stories everywhere

---
 Telegram/CMakeLists.txt                       |  2 +
 .../SourceFiles/data/data_media_types.cpp     | 11 +++
 Telegram/SourceFiles/data/data_stories.cpp    |  9 +-
 .../media/view/media_view_overlay_widget.cpp  |  4 +
 .../SourceFiles/settings/settings_tele.cpp    | 91 +++++++++++++++++++
 Telegram/SourceFiles/tele/tele_options.cpp    | 26 ++++++
 Telegram/SourceFiles/tele/tele_options.h      |  9 ++
 7 files changed, 151 insertions(+), 1 deletion(-)
 create mode 100644 Telegram/SourceFiles/tele/tele_options.cpp
 create mode 100644 Telegram/SourceFiles/tele/tele_options.h

diff --git a/Telegram/CMakeLists.txt b/Telegram/CMakeLists.txt
index 6d2111f94e0326b6e09d75f43def44780ab6aad5..56308300b9fb3503cdf53b2f9f6ee8b3bbe5b759 100644
--- a/Telegram/CMakeLists.txt
+++ b/Telegram/CMakeLists.txt
@@ -2215,6 +2215,8 @@ PRIVATE
     settings/settings_tele.h
     tele/tele_build.cpp
     tele/tele_build.h
+    tele/tele_options.cpp
+    tele/tele_options.h
 )
 set_source_files_properties(${src_loc}/tele/tele_build.cpp
 PROPERTIES
diff --git a/Telegram/SourceFiles/data/data_media_types.cpp b/Telegram/SourceFiles/data/data_media_types.cpp
index 3f64b411f4050a60035aa095e4a0727fee9e96c9..eaee27a4acb55fe54e3030c9537a6f1d11b4219b 100644
--- a/Telegram/SourceFiles/data/data_media_types.cpp
+++ b/Telegram/SourceFiles/data/data_media_types.cpp
@@ -82,6 +82,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "storage/file_upload.h"
 #include "window/window_session_controller.h" // SessionController::uiShow.
 #include "apiwrap.h"
+#include "tele/tele_options.h"
 #include "styles/style_chat.h"
 #include "styles/style_dialogs.h"
 
@@ -2980,6 +2981,9 @@ FullStoryId MediaStory::storyId() const {
 }
 
 bool MediaStory::storyExpired(bool revalidate) {
+	if (Tele::HideStories()) {
+		return true;
+	}
 	if (revalidate) {
 		const auto stories = &parent()->history()->owner().stories();
 		if (const auto maybeStory = stories->lookup(_storyId)) {
@@ -3057,6 +3061,13 @@ std::unique_ptr<HistoryView::Media> MediaStory::createView(
 		not_null<HistoryView::Element*> message,
 		not_null<HistoryItem*> realParent,
 		HistoryView::Element *replacing) {
+	if (Tele::HideStories()) {
+		if (!_mention) {
+			realParent->setText(TextWithEntities());
+		}
+		_expired = true;
+		return nullptr;
+	}
 	const auto spoiler = false;
 	const auto stories = &parent()->history()->owner().stories();
 	const auto maybeStory = stories->lookup(_storyId);
diff --git a/Telegram/SourceFiles/data/data_stories.cpp b/Telegram/SourceFiles/data/data_stories.cpp
index a1d5d89580b06cbee04217ef4d00269cd2e7aa76..03f5d38068ec4e8a131e7a47e73959dd94c31389 100644
--- a/Telegram/SourceFiles/data/data_stories.cpp
+++ b/Telegram/SourceFiles/data/data_stories.cpp
@@ -26,6 +26,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "main/main_session.h"
 #include "ui/layers/show.h"
 #include "ui/text/text_utilities.h"
+#include "tele/tele_options.h"
 
 namespace Data {
 namespace {
@@ -460,7 +461,7 @@ void Stories::parseAndApply(
 			}
 		}
 	}
-	if (result.ids.empty()) {
+	if (result.ids.empty() || Tele::HideStories()) {
 		applyDeletedFromSources(peerId, StorySourcesList::NotHidden);
 		applyDeletedFromSources(peerId, StorySourcesList::Hidden);
 		peer->setStoriesState(PeerData::StoriesState::None);
@@ -1779,6 +1780,9 @@ void Stories::albumIdsLoadMore(PeerId peerId, int albumId) {
 
 void Stories::albumIdsLoadMore(PeerId peerId, int albumId, bool reload) {
 	Expects(!reload || albumId > 0);
+	if (Tele::HideStories()) {
+		return;
+	}
 
 	const auto peer = _owner->peer(peerId);
 	const auto set = albumIdsSet(peerId, albumId);
@@ -2346,6 +2350,9 @@ void Stories::setPreloadingInViewer(std::vector<FullStoryId> ids) {
 std::optional<Stories::PeerSourceState> Stories::peerSourceState(
 		not_null<PeerData*> peer,
 		const MTPRecentStory &recent) {
+	if (Tele::HideStories()) {
+		return PeerSourceState();
+	}
 	const auto &data = recent.data();
 	const auto maxId = data.vmax_id().value_or_empty();
 	const auto live = data.is_live();
diff --git a/Telegram/SourceFiles/media/view/media_view_overlay_widget.cpp b/Telegram/SourceFiles/media/view/media_view_overlay_widget.cpp
index f57d4314b916556c5610c9a8633ebc2b580b8a51..36e08897723f019b4fc4eea37e504e1b4be1530f 100644
--- a/Telegram/SourceFiles/media/view/media_view_overlay_widget.cpp
+++ b/Telegram/SourceFiles/media/view/media_view_overlay_widget.cpp
@@ -117,6 +117,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "styles/style_menu_icons.h"
 #include "styles/style_ttl_media.h"
 #include "platform/platform_text_recognition.h"
+#include "tele/tele_options.h"
 
 #include <QGraphicsOpacityEffect>
 
@@ -4517,6 +4518,9 @@ void OverlayWidget::activate() {
 
 void OverlayWidget::show(OpenRequest request) {
 	const auto story = request.story();
+	if (story && Tele::HideStories()) {
+		return;
+	}
 	const auto document = story ? story->document() : request.document();
 	const auto photo = story ? story->photo() : request.photo();
 	const auto call = story ? story->call() : request.call();
diff --git a/Telegram/SourceFiles/settings/settings_tele.cpp b/Telegram/SourceFiles/settings/settings_tele.cpp
index 56e5327dc7f466f2f5c3216565593299a91b69d1..431ad10bb25286c95b4eb8eba8b960248731dd6c 100644
--- a/Telegram/SourceFiles/settings/settings_tele.cpp
+++ b/Telegram/SourceFiles/settings/settings_tele.cpp
@@ -1,11 +1,20 @@
 #include "settings/settings_tele.h"
 
+#include "base/options.h"
+#include "base/timer.h"
+#include "core/application.h"
 #include "core/version.h"
+#include "lang/lang_keys.h"
 #include "settings/settings_builder.h"
 #include "settings/settings_common_session.h"
 #include "tele/tele_build.h"
+#include "tele/tele_options.h"
+#include "ui/boxes/confirm_box.h"
 #include "ui/vertical_list.h"
+#include "ui/widgets/buttons.h"
+#include "ui/widgets/labels.h"
 #include "ui/wrap/vertical_layout.h"
+#include "window/window_session_controller.h"
 #include "styles/style_menu_icons.h"
 #include "styles/style_settings.h"
 
@@ -25,6 +34,83 @@ private:
 
 };
 
+[[nodiscard]] not_null<Button*> AddOptionRow(
+		not_null<Ui::VerticalLayout*> container,
+		const QString &name,
+		const QString &description) {
+	const auto &titlePadding = st::settingsExperimentalTitlePadding;
+	const auto &aboutPadding = st::settingsExperimentalAboutPadding;
+	const auto button = Ui::CreateChild<Button>(
+		container.get(),
+		rpl::single(QString()),
+		st::settingsButtonNoIcon);
+	const auto title = container->add(
+		object_ptr<Ui::FlatLabel>(
+			container,
+			name,
+			st::settingsExperimentalTitle),
+		titlePadding);
+	const auto about = container->add(
+		object_ptr<Ui::FlatLabel>(
+			container,
+			description,
+			st::settingsExperimentalAbout),
+		aboutPadding);
+	title->setAttribute(Qt::WA_TransparentForMouseEvents);
+	about->setAttribute(Qt::WA_TransparentForMouseEvents);
+	rpl::combine(
+		container->widthValue(),
+		title->heightValue(),
+		about->heightValue()
+	) | rpl::on_next([=](int width, int titleHeight, int aboutHeight) {
+		button->resize(width, titlePadding.top()
+			+ titleHeight
+			+ titlePadding.bottom()
+			+ aboutPadding.top()
+			+ aboutHeight
+			+ aboutPadding.bottom());
+	}, button->lifetime());
+	title->topValue(
+	) | rpl::on_next([=](int top) {
+		button->moveToLeft(0, top - titlePadding.top());
+	}, button->lifetime());
+	button->show();
+	return button;
+}
+
+void AddOptionToggle(
+		not_null<Ui::VerticalLayout*> container,
+		not_null<Window::SessionController*> controller,
+		const char id[]) {
+	auto &option = base::options::lookup<bool>(id);
+	const auto button = AddOptionRow(
+		container,
+		option.name(),
+		option.description()
+	)->toggleOn(rpl::single(option.value()));
+
+	const auto restarter = option.restartRequired()
+		? button->lifetime().make_state<base::Timer>()
+		: nullptr;
+	if (restarter) {
+		restarter->setCallback([=] {
+			controller->show(Ui::MakeConfirmBox({
+				.text = tr::lng_settings_need_restart(),
+				.confirmed = [] { Core::Restart(); },
+				.confirmText = tr::lng_settings_restart_now(),
+				.cancelText = tr::lng_settings_restart_later(),
+			}));
+		});
+	}
+	button->toggledChanges(
+	) | rpl::on_next([=, &option](bool toggled) {
+		option.set(toggled);
+		if (restarter) {
+			restarter->callOnce(st::settingsButtonNoIcon.toggle.duration);
+		}
+	}, button->lifetime());
+}
+
 TeleSection::TeleSection(
 	QWidget *parent,
 	not_null<Window::SessionController*> controller)
@@ -39,6 +125,11 @@ rpl::producer<QString> TeleSection::title() {
 void TeleSection::setupContent() {
 	const auto content = Ui::CreateChild<Ui::VerticalLayout>(this);
 
+	Ui::AddSkip(content);
+	Ui::AddSubsectionTitle(content, rpl::single(u"Stories"_q));
+	AddOptionToggle(content, controller(), ::Tele::kOptionHideStories);
+	Ui::AddSkip(content);
+
 	Ui::AddDividerText(
 		content,
 		rpl::single(u"Telegram Desktop %1 with tele patches, build %2."_q.arg(
diff --git a/Telegram/SourceFiles/tele/tele_options.cpp b/Telegram/SourceFiles/tele/tele_options.cpp
new file mode 100644
index 0000000000000000000000000000000000000000..335ebec14f07215103e5c57caa960ef681dd8175
--- /dev/null
+++ b/Telegram/SourceFiles/tele/tele_options.cpp
@@ -0,0 +1,26 @@
+#include "tele/tele_options.h"
+
+#include "base/options.h"
+
+namespace Tele {
+
+const char kOptionHideStories[] = "tele-hide-stories";
+
+namespace {
+
+base::options::toggle HideStoriesOption({
+	.id = kOptionHideStories,
+	.name = "Hide stories",
+	.description = "No stories anywhere: no stories bar, no rings around "
+		"userpics, no stories in profiles, story messages show "
+		"as expired and story links don't open.",
+	.restartRequired = true,
+});
+
+} // namespace
+
+bool HideStories() {
+	return HideStoriesOption.value();
+}
+
+} // namespace Tele
diff --git a/Telegram/SourceFiles/tele/tele_options.h b/Telegram/SourceFiles/tele/tele_options.h
new file mode 100644
index 0000000000000000000000000000000000000000..97ed2f6363b47168ae58db39fd7122bd03625d8c
--- /dev/null
+++ b/Telegram/SourceFiles/tele/tele_options.h
@@ -0,0 +1,9 @@
+#pragma once
+
+namespace Tele {
+
+extern const char kOptionHideStories[];
+
+[[nodiscard]] bool HideStories();
+
+} // namespace Tele
