name: Build

on:
  push:
    branches: [main]
    paths:
      - UPSTREAM
      - 'patches/**'
      - tele.py
      - 'ci/**'
      - .github/workflows/build.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build

jobs:
  windows:
    name: Windows x64
    runs-on: windows-latest
    timeout-minutes: 360
    permissions:
      contents: read
      issues: write
    outputs:
      tag: ${{ steps.number.outputs.tag }}
      base: ${{ steps.number.outputs.base }}
      counter: ${{ steps.number.outputs.counter }}
      upstream: ${{ steps.number.outputs.upstream }}
    defaults:
      run:
        shell: cmd

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          path: tele
          persist-credentials: false

      - name: Check secrets
        shell: bash
        env:
          API_ID: ${{ secrets.TDESKTOP_API_ID }}
          API_HASH: ${{ secrets.TDESKTOP_API_HASH }}
          UPDATE_KEY: ${{ secrets.TELE_UPDATE_KEY }}
        run: |
          if [ -z "$API_ID" ] || [ -z "$API_HASH" ]; then
            echo "::error::set the TDESKTOP_API_ID and TDESKTOP_API_HASH repository secrets"
            exit 1
          elif [ -z "$UPDATE_KEY" ]; then
            echo "::error::set the TELE_UPDATE_KEY repository secret"
            exit 1
          fi

      - name: Prepare directories
        run: |
          mkdir %USERPROFILE%\TBuild\Libraries
          mklink /d %GITHUB_WORKSPACE%\TBuild %USERPROFILE%\TBuild
          echo TBUILD=%GITHUB_WORKSPACE%\TBuild>>%GITHUB_ENV%

      - name: Clone tdesktop
        shell: bash
        run: |
          tag=$(tr -d '[:space:]' < tele/UPSTREAM)
          echo "UPSTREAM_TAG=$tag" >> "$GITHUB_ENV"
          git -c advice.detachedHead=false clone --quiet --recurse-submodules --branch "$tag" \
            https://github.com/telegramdesktop/tdesktop.git "$TBUILD/tdesktop"

      - name: Pick the release number
        id: number
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          base="$UPSTREAM_TAG-tele"
          pattern="^${base//./\\.}\.\([0-9][0-9]*\)$"
          last=$( {
            gh release list --limit 1000 --json tagName --jq '.[].tagName'
            git ls-remote --tags "$GITHUB_SERVER_URL/$GH_REPO.git" | sed -n 's|^[0-9a-f]*\trefs/tags/||p'
          } | sed -n "s/$pattern/\1/p" | sort -n | tail -n1)
          number=$(( ${last:-0} + 1 ))
          version=$(tr -d '\r' < "$TBUILD/tdesktop/Telegram/build/version" | sed -n 's/^AppVersion *\([0-9][0-9]*\)$/\1/p')
          if [ -z "$version" ]; then
            echo "::error::no AppVersion in Telegram/build/version"
            exit 1
          fi
          echo "TELE_BUILD=$number" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=$base.$number" >> "$GITHUB_ENV"
          {
            echo "tag=$base.$number"
            echo "base=$version"
            echo "counter=$number"
            echo "upstream=$UPSTREAM_TAG"
          } >> "$GITHUB_OUTPUT"

      - name: Apply patches
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          python tele\tele.py --tdesktop %TBUILD%\tdesktop apply

      - name: Read toolchain versions
        shell: bash
        run: |
          doc="$TBUILD/tdesktop/docs/building-win.md"
          sdk=$(grep -oE '\*\*[0-9.]+\*\* SDK version' "$doc" | grep -oE '[0-9][0-9.]+' | head -n1 || true)
          # docs before feb 2026 don't pin a toolset
          toolset=$(grep -oE 'vcvars_ver=[0-9.]+' "$doc" | head -n1 | cut -d= -f2 || true)
          if [ -z "$sdk" ]; then
            echo "::error::no windows sdk version in docs/building-win.md"
            exit 1
          fi
          echo "SDK=$sdk" >> "$GITHUB_ENV"
          echo "VCVARS_VER=$toolset" >> "$GITHUB_ENV"
          # through stdin: a backslash in the path makes sha256sum prefix its output with \
          prepare_hash=$(sha256sum < "$TBUILD/tdesktop/Telegram/build/prepare/prepare.py" | cut -c1-16)
          echo "CACHE_KEY=win64-$sdk-$prepare_hash" >> "$GITHUB_ENV"
          # same as upstream ci: no /Zi in the libraries' debug builds, keeps the cache small
          sed -i '/CMAKE_${lang}_FLAGS_DEBUG_INIT/s/${_Zi}//' "$PROGRAMFILES"/CMake/share/cmake*/Modules/Platform/Windows-MSVC.cmake

      - name: Restore libraries
        uses: actions/cache/restore@v6
        with:
          path: |
            ${{ env.TBUILD }}\ThirdParty
            ${{ env.TBUILD }}\Libraries
          key: ${{ env.CACHE_KEY }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            ${{ env.CACHE_KEY }}-
            win64-

      - name: Remember library state
        shell: bash
        run: |
          state=$( { find "$TBUILD/ThirdParty/cache_keys" "$TBUILD/Libraries/win64/cache_keys" -type f -exec sha256sum {} + 2>/dev/null || true; } | sort | sha256sum)
          echo "LIBS_STATE=$state" >> "$GITHUB_ENV"

      - name: NuGet sources
        run: |
          nuget sources Disable -Name "Microsoft Visual Studio Offline Packages"
          nuget sources Add -Source https://api.nuget.org/v3/index.json & exit 0

      # the image has the 14.44 toolset without its atl, and breakpad's dump_syms (release libs only) needs it
      - name: Install ATL for the MSVC toolset
        if: env.VCVARS_VER != ''
        timeout-minutes: 20
        shell: pwsh
        run: |
          $prepared = "$env:TBUILD\Libraries\win64\cache_keys\tele-prepared"
          if ((Test-Path $prepared) -and (Get-Content $prepared -Raw).Trim() -eq $env:CACHE_KEY) {
            "libraries were fully prepared for this prepare.py, atl is not needed"; exit 0
          }
          $components = @{ '14.44' = 'Microsoft.VisualStudio.Component.VC.14.44.17.14.ATL' }
          $installer = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer"
          $vs = & "$installer\vswhere.exe" -latest -products * -property installationPath
          $atl = "$vs\VC\Tools\MSVC\$env:VCVARS_VER.*\atlmfc\include\atlbase.h"
          if (Test-Path $atl) { "atl for $env:VCVARS_VER is installed"; exit 0 }
          $component = $components[$env:VCVARS_VER]
          if (-not $component) { "::error::no known atl component for toolset $env:VCVARS_VER"; exit 1 }
          $process = Start-Process -Wait -PassThru "$installer\vs_installer.exe" -ArgumentList `
            'modify', '--installPath', "`"$vs`"", '--add', $component, '--quiet', '--norestart', '--nocache'
          if ($process.ExitCode -ne 0 -and $process.ExitCode -ne 3010) { exit $process.ExitCode }
          if (-not (Test-Path $atl)) { "::error::$component did not install $atl"; exit 1 }
          "installed $component"

      - name: Prepare libraries
        id: prepare
        run: |
          call tele\ci\msvc.cmd || exit /b 1
          %TBUILD%\tdesktop\Telegram\build\prepare\win.bat silent

      - name: Trim libraries
        id: trim
        if: always() && steps.prepare.outcome != 'skipped'
        shell: bash
        env:
          PREPARE_OUTCOME: ${{ steps.prepare.outcome }}
        run: |
          libs="$TBUILD/Libraries/win64"
          if [ ! -d "$libs" ]; then
            exit 0
          fi
          # upstream ci's filter: keep what linking needs, drop sources and intermediates
          find "$libs" '(' '(' ! '(' -name '*.lib' -o -name '*.a' -o -name '*.exe' -o -name '*.h' -o -name '*.hpp' -o -name '*.inc' -o -name '*.cmake' -o -name '*.pc' -o -path '*/include/*' -o -path '*/objects-*' -o -path '*/cache_keys/*' -o -path '*/patches/*' -o -path '*/nv-codec-headers/*' ')' -type f ')' -o -empty ')' -delete
          # the atl step only trusts a prepare that fully finished for this CACHE_KEY
          if [ "$PREPARE_OUTCOME" = success ]; then
            echo "$CACHE_KEY" > "$libs/cache_keys/tele-prepared"
          else
            rm -f "$libs/cache_keys/tele-prepared"
          fi
          state=$( { find "$TBUILD/ThirdParty/cache_keys" "$libs/cache_keys" -type f -exec sha256sum {} + 2>/dev/null || true; } | sort | sha256sum)
          if [ "$state" != "$LIBS_STATE" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Save libraries
        if: always() && steps.trim.outputs.changed == 'true'
        uses: actions/cache/save@v6
        with:
          path: |
            ${{ env.TBUILD }}\ThirdParty
            ${{ env.TBUILD }}\Libraries
          key: ${{ env.CACHE_KEY }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Build Telegram
        env:
          API_ID: ${{ secrets.TDESKTOP_API_ID }}
          API_HASH: ${{ secrets.TDESKTOP_API_HASH }}
        run: |
          call tele\ci\msvc.cmd || exit /b 1
          cd /d %TBUILD%\tdesktop\Telegram
          call configure.bat -G "Ninja Multi-Config" -D CMAKE_CONFIGURATION_TYPES=Release -D CMAKE_MSVC_DEBUG_INFORMATION_FORMAT= -D TDESKTOP_API_ID=%API_ID% -D TDESKTOP_API_HASH=%API_HASH% -D TELE_BUILD=%TELE_BUILD% || exit /b 1
          cmake --build ..\out --config Release --parallel --target Telegram

      - name: Package
        shell: bash
        run: 7z a -tzip "tele-$RELEASE_TAG-win64.zip" "$TBUILD/tdesktop/out/Release/tele.exe" > /dev/null

      - name: Upload the build
        uses: actions/upload-artifact@v7
        with:
          name: release
          path: tele-${{ env.RELEASE_TAG }}-win64.zip
          if-no-files-found: error
          retention-days: 1

      - name: Report failure
        if: failure() || cancelled()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          echo "Building ${UPSTREAM_TAG:-UPSTREAM} failed: $GITHUB_SERVER_URL/$GH_REPO/actions/runs/$GITHUB_RUN_ID" > issue.md
          bash tele/ci/issue.sh open build-failure "Build failed: ${UPSTREAM_TAG:-UPSTREAM}" issue.md

  publish:
    name: Publish
    needs: windows
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    env:
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
      TAG: ${{ needs.windows.outputs.tag }}
      BASE: ${{ needs.windows.outputs.base }}
      COUNTER: ${{ needs.windows.outputs.counter }}
      UPSTREAM_TAG: ${{ needs.windows.outputs.upstream }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          path: tele
          persist-credentials: false

      - name: Download the build
        uses: actions/download-artifact@v8
        with:
          name: release

      - name: Sign the update feed
        env:
          UPDATE_KEY: ${{ secrets.TELE_UPDATE_KEY }}
        run: |
          zip="tele-$TAG-win64.zip"
          printf '{"tag":"%s","base":%s,"counter":%s,"url":"%s","zip_sha256":"%s","exe_sha256":"%s"}' \
            "$TAG" "$BASE" "$COUNTER" \
            "$GITHUB_SERVER_URL/$GH_REPO/releases/download/$TAG/$zip" \
            "$(sha256sum < "$zip" | cut -c1-64)" \
            "$(unzip -p "$zip" tele.exe | sha256sum | cut -c1-64)" > feed.json
          openssl pkeyutl -sign -rawin -inkey <(printf '%s\n' "$UPDATE_KEY") -in feed.json -out feed.sig
          openssl pkeyutl -verify -rawin -pubin -inkey tele/ci/update-public-key.pem -in feed.json -sigfile feed.sig
          printf '{"feed":"%s","signature":"%s"}\n' "$(base64 -w0 feed.json)" "$(base64 -w0 feed.sig)" > tele-update-win64.json

      - name: Publish release
        run: |
          zip="tele-$TAG-win64.zip"
          patches=$(cd tele && { find patches -name '*.patch' 2>/dev/null || true; } | sort)
          {
            echo "Telegram Desktop [$UPSTREAM_TAG](https://github.com/telegramdesktop/tdesktop/releases/tag/$UPSTREAM_TAG) with these patches:"
            echo
            if [ -z "$patches" ]; then
              echo "- none, this is upstream as is"
            fi
            for patch in $patches; do
              echo "- [$patch]($GITHUB_SERVER_URL/$GH_REPO/blob/$GITHUB_SHA/$patch)"
            done
          } > notes.md
          gh release create "$TAG" "$zip" tele-update-win64.json --latest --target "$GITHUB_SHA" --title "$TAG" --notes-file notes.md
          bash tele/ci/issue.sh close build-failure "Built $TAG."

      - name: Report failure
        if: failure() || cancelled()
        run: |
          echo "Publishing ${TAG:-the build} failed: $GITHUB_SERVER_URL/$GH_REPO/actions/runs/$GITHUB_RUN_ID" > issue.md
          bash tele/ci/issue.sh open build-failure "Publish failed: ${TAG:-unknown}" issue.md
